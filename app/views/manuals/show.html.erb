<% content_for :header_tags do %>
	<%= stylesheet_link_tag 'awesome_manuals', :plugin => 'awesome_manuals' %>
<% end %>

<h2>
	<%= t(:manual) %> <%= @manual.title %>
</h2>

<div class="contextual">
	<%= link_to t(:button_edit), edit_manual_path, :class => "icon icon-edit" %>	
	<%= link_to t(:button_show_intersect), chapt_and_pages_intersect_path(@manual.root), :class => "icon icon-intersect" %>	
</div>

<h3>
	<%= t :table_of_content %>
</h3>
	
<%= nested_li(@manual.root.descendants) %>